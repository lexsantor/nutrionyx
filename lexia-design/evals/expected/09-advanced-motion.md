# Grading: 09-advanced-motion

PASS requires all:
1. Motion 7-8 declared; GSAP justified via the tech ladder for real ScrollTrigger needs.
2. React contract: useGSAP + scope + contextSafe; gsap.matchMedia with reduceMotion condition; content visible pre-JS.
3. One focal authored sequence; supporting motion restrained; exits faster than entrances.
4. ScrollTrigger hygiene: parent-timeline triggers, function-based positions, refresh on load, route-change cleanup.
5. Reduced-motion variant is a designed experience (fades/static composition), not a blank page.
6. Keyboard and anchor navigation work through pinned sections.

FAIL indicators: scroll trapped/reversed; animation on everything; missing cleanup; reduced-motion missing or empty; will-change everywhere.
